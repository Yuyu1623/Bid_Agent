智能体分为六层，分别是文件解析层，结构化抽取层，知识沉淀层，生成层，审查层和辅助决策层。
1）招标文件解析层（招标文件分类后，调用对应的解析工具走智能解析）
在文件解析层，规定无论哪一种输入类型，最终解析结果统一为 Markdown + 结构化元数据（标题层级、表格 JSON、图片描述） 的中间格式。

2）结构化抽取层，大体采用（规则召回 + 向量召回），然后并行调用大模型进行抽取。
规则召回：（稀疏检索）按宽关键词和章节标题召回候选片段，再做规则重排、噪声扣分和 Top 候选截断，减少每个模块重复读取全文
向量召回： 对 document_chunks 提前做 embedding，对每个抽取模块（如“商务要求”）用模块描述作为 query 做语义检索召回 TopK 片段，与规则召回结果融合。大模型调用分为两步：第一步先调用大模型，输出完整的project_profile（项目名称、编号、预算、招标人、代理机构、采购方式、是否分包等）以及全文的章节树（章节标题、起始位置、层级关系）。之后各路专项抽取都带着这份project_profile 和章节树作为上下文，要求模型“不得修改项目基本信息，只能从指定章节范围内提取”，从源头减少冲突。

废标项 / 资格要求的全覆盖（预筛+精提取，不做全量检索）
废标项和资格审查有时隐藏在非独立章节（如“投标人须知前附表”“评审办法”），如果只按标题召回容易漏。
可以为这两个模块配置“全量检索 + 高风险段落优先”的策略，或者单独做一次全文档扫描（可以用较长 context 的大模型一次性扫描，也可以逐章分段让小模型标出“疑似废标/资格审查”段落再做精提取）。

目前采用：逐章分段让小模型标出“疑似废标/资格审查”段落再做精提取

-> 轻量的内容审查智能体
      -> 正则检查各模块关键字段覆盖率
      -> 区分“原文存在但提取缺失”“提取存在但原文未命中”“原文和提取均命中”
      -> 回查原文证据片段，辅助人工快速定位
      -> 对提取结果做原文溯源评分
      -> 可选大模型深度审查，输出高风险问题和人工核对建议

-> 清洗标准化
  -> 输出质量后处理
      -> 表格行去重
      -> 句子 / 小点去重
      -> 《材料名称》去重
      -> 金额、时间、日期标准化写入 metadata
      -> 商务要求与技术要求、评分项建立跨模块引用
      -> 原子条目拆分后入库

-> 清洗标准化后结构化进SQLite 以及 Chrome向量数据库。
只靠 prompt 里的文字说明，大模型偶尔还是会输出合并段落、编号变形或者漏掉必填字段。
改正方法：彻底使用 JSON Schema / Function Calling
为每个模块定义严格的 JSON 对象数组结构，借助模型的 tool_use / function_call 能力强制输出已解析的结构化对象，而不是自由文本后再用正则解析。例如评分项：
{
  "type": "技术评分",
  "item": "项目实施方案",
  "standard": "根据方案的完整性、合理性、可操作性评分",
  "max_score": 10,
  "evidence_snippet": "详见第三章..."
}
这样可以直接入库，无需再写容易出错的文本清洗正则。
每个原子条目里增加 source_chunk_id 或 source_text 字段，强制模型回指原文片段。这不仅方便溯源，还能在后期人工审核时一键定位，也能作为自动化测试的真实标签（golden evidence）。
比如 review_findings 的风险等级（高/中/低）、待处理状态（open/resolved/ignored）、废标项的法律性质（资格性/符合性）等，在 schema 里用 enum 约束。这样下游统计和看板才能归一化。

实际技术路径：
-> 第一次先调用大模型，只生成全局 project_profile 和全文 section_tree，统一项目名称、编号、预算、招标人、代理机构、采购方式、分包等基础字段
-> section_tree 记录章节标题、层级、起始位置/页码、标题路径和模块线索，作为后续专项抽取的结构锚点
-> 专项抽取阶段带着 project_profile 和 section_tree 作为上下文，并要求模型不得修改项目基本信息，只能从指定候选章节范围内提取
-> 商务内容、技术要求、资格审查、评分要求四路并发调用大模型，减少项目基础字段冲突
-> 资格审查 / 废标项增加轻量模型逐章预筛：先标出疑似资格、符合性审查、废标/否决投标段落，再并入资格审查精提取上下文，提升全覆盖率